import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  list: {
    flexGrow: 1,
    paddingBottom: 20,
  },
  emptyText: {
    textAlign: "center",
    color: "#7f8c8d",
    fontSize: 16,
    marginTop: 20,
    fontStyle: "italic",
  },
});